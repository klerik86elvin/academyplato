<div class="ambassador-item">
    <div class="content">
        <img src="{{ Voyager::image( $d->image) }}" alt="">
        <p class="name">{{$d->name}}</p>
        <p class="position">{{$d->position}}</p>
        <hr>
        <p class="text-title">{{$d->text_title}}</p>
        <p class="text">{{$d->text}}</p>
    </div>
</div>
